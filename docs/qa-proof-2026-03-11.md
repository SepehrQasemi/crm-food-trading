# QA Proof - 2026-03-11

Repository: `C:\dev\crm-food-trading`  
Validation date: 2026-03-11  
Validation scope: lint + build + unit + component + API + full E2E + coverage

## Commands Executed (Latest Run)

1. `npm run test:release`
- Result: PASS
- Includes: `lint` + `build` + `test` + full Playwright suite.

2. `npm --workspace web run test:coverage`
- Result: PASS
- Coverage thresholds gate: PASS (`scripts/check-coverage.mjs`).

3. `npm --workspace web run test:e2e -- --project=mobile-chromium`
- Result: PASS
- Mobile project summary: 3 passed, 14 skipped (expected skips for desktop-only scenarios).

## Detailed Evidence

### A) Lint / Build
- `npm run lint`: PASS
- `npm run build`: PASS
- Build generated all expected app routes including:
  - core pages (`/dashboard`, `/companies`, `/contacts`, `/products`, `/leads`, `/tasks`, `/emails`, `/access`, `/profile`, `/help`, `/settings`)
  - auth pages (`/login`, `/auth/callback`, `/reset-password`)
  - category pages (`/categories`, `/categories/[id]`)
  - all main API routes.

### B) Unit + Component + API Tests
- `npm run test`: PASS
- Unit tests: 25 passed
- Component tests: 7 passed
- API tests: 39 passed

### C) Full End-to-End (Playwright)
- `npm --workspace web run test:e2e:full`: PASS
- Result summary:
  - 17 passed
  - 17 skipped (expected matrix skips between desktop-only and mobile-only cases)
- Covered flows:
  - auth/reset entry flow
  - dashboard KPIs and export
  - dashboard scope rules (`own` vs `team`) for role-based visibility
  - weighted forecast calendar values from lead stage probabilities
  - companies/contacts/products/leads/tasks/emails end-to-end
  - negotiation stage dual outcome actions (`Mark Won` / `Mark Lost`)
  - lead create UX with fixed source list + contact/company type-ahead suggestions
  - lead assignment governance (creator default, admin reassignment)
  - won/lost stage pagination (5 cards/page)
  - product relations + customer finder
  - dedicated mobile responsive regression suite:
    - core mobile pages navigation
    - horizontal overflow guard
    - mobile email compose field width usability
  - role-based access scenarios
  - notification bell flow
  - mobile navigation smoke.

### D) Coverage
- `npm --workspace web run test:coverage`: PASS
- Total:
  - Statements: 98.96%
  - Branches: 74.55%
  - Functions: 89.47%
  - Lines: 98.96%
- Threshold script result: `[coverage] thresholds passed.`

## Functional Risk Checks

1. Password UX hardening
- Signup requires password confirmation.
- Password fields support show/hide toggle on login, reset-password, and settings.
- Settings page is minimal and dedicated to password reset.

2. Reset password robustness
- Recovery URL variants supported:
  - `code`
  - `token_hash` + `type=recovery`
  - hash tokens (`access_token`, `refresh_token`)
- Recovery redirect path uses:
  - `/reset-password` (same-origin redirect)
- Rate-limit UX for reset email is handled with clear cooldown messaging.

3. Access model
- Shared read visibility for small-team mode validated on:
  - contacts
  - companies
  - products
  - leads
- Edit/delete restrictions remain role-aware per module.

## Conclusion
All quality gates passed on 2026-03-11 with full release test execution and coverage gate.  
Project status: release-ready for professor review and oral defense.
